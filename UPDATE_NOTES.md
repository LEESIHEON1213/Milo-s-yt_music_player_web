# 이번 업데이트 요약 (스탠다드 버전 기준)

디자인은 첨부한 스탠다드 버전과 동일하게 유지하고, 기능/버튼/백엔드 로직만 추가·수정했습니다.

## 핵심 변경 (App.jsx)
- **대기열 자동재생 핵심 룰**: 어느 탭(검색/랭킹/즐겨찾기/대기열)에서 추가하든, 재생 중인 곡이 없을 때 곡이 들어오면 **무조건 첫 곡부터 자동재생**하고 이후 **순서대로 전부 재생**합니다. (`addToQueue`, `enqueueMany`, `startingRef` 가드로 state 비동기 race 방지)
- **곡 없을 때 컨트롤 가드**: 재생 중인 곡이 없으면 셔플/이전/재생/다음/반복 버튼과 시크바가 비활성화되고, 누르면 "곡을 재생해주세요" 안내가 뜹니다.
- **+ 대기열 추가 버튼**: 검색 결과, 랭킹(Top3 + 카테고리 랭킹), 즐겨찾기 모든 섹션에 추가.
- **즐겨찾기 전체 일괄 대기열 추가 버튼**: 즐겨찾기 탭 상단에 추가. 비어있을 때 비활성화.
- 링크(단일 URL) 검색 시 바로 재생하지 않고 ▶/➕ 선택 가능하게 결과로 표시.

## 재생 횟수 표시 (CategoryRanking)
- 재생수를 **제목 오른쪽 고정 세로열(전용 컬럼)**에 배치 → 곡 제목 길이와 무관하게 항상 같은 위치 정렬.
- 모바일에선 전용 컬럼 대신 제목 줄 옆 작은 배지로 표시(정보 손실 방지).

## 모바일 UI/UX 최적화
- 데스크톱 디자인 톤은 그대로 두고, ≤600px에서 레이아웃만 모바일 친화적으로 재배치:
  - 탭 2x2 그리드, 컨트롤 버튼 줄바꿈 + 큰 터치영역, 재생 버튼 풀폭
  - 검색 결과 1열 가로 카드, 검색폼 세로 스택
  - 랭킹 테이블 컴팩트 정렬, Top3 오버레이 상시 표시(터치엔 호버 없음)

## 백엔드
- 변경 없음. 재생수는 기존대로 `/api/resolve` 호출 시 자동 기록됩니다.

## 배포 (ARM64에서 rollup 에러 시)
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run build
```
